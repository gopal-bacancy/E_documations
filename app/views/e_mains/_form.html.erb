<%= form_with(model: e_main) do |form| %>
  <% if e_main.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(e_main.errors.count, "error") %> prohibited this e_main from being saved:</h2>

      <ul>
        <% e_main.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
